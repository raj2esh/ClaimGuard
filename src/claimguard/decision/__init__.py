"""ClaimGuard decision package (Step 16): a deterministic, non-learned
evidence-decision policy layered on top of the Step 15 integration
pipeline's verified candidates. The binary verifier itself is never
modified here - see `claimguard.decision.policy` and
`claimguard.decision.calibration`."""
